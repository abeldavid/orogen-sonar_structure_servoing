# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(sonar_structure_servoingTaskLib)
ADD_LIBRARY(${SONAR_STRUCTURE_SERVOING_TASKLIB_NAME} SHARED 
    ${SONAR_STRUCTURE_SERVOING_TASKLIB_SOURCES})
add_dependencies(${SONAR_STRUCTURE_SERVOING_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SONAR_STRUCTURE_SERVOING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SONAR_STRUCTURE_SERVOING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SONAR_STRUCTURE_SERVOING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SONAR_STRUCTURE_SERVOING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SONAR_STRUCTURE_SERVOING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SONAR_STRUCTURE_SERVOING_TASKLIB_HEADERS}
    DESTINATION include/orocos/sonar_structure_servoing)

